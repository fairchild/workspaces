# Lanes a workflow may legitimately target. The list is empty: this repo has no
# self-hosted runner. `tart-ui` went with
# docs/decisions/perf-measurement-laptop-optin.md, `lume-macos` with the #1288
# follow-up (agent evidence moved to hosted macos-15), and `signing-host` with
# the deregistration of `blue-workspaces` on 2026-08-13. Any `runs-on` naming a
# self-hosted label now fails lint rather than queueing against nothing.
# Re-provisioning a lane means adding its label back here first — see
# docs/development/signing-runner-setup.md.
self-hosted-runner:
  labels: []

config-variables: null
