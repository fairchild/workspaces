# Ops Telemetry

Live monitor telemetry is written to the unprotected `factory/ops-data` branch under
`docs/ops/`. The files on `main` are historical snapshots because `main` is PR-only
and GitHub Actions cannot commit telemetry updates there directly.
