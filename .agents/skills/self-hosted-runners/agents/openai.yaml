interface:
  display_name: "Self-Hosted Runners"
  short_description: "Reason about this repo's retired self-hosted lanes."
  default_prompt: "Use $self-hosted-runners to inspect the current GitHub runner inventory, classify why a job is queued against a self-hosted label, and explain the next action — re-provisioning a retired lane is a deliberate revert, not a recovery step."
